_progressBarColor1 = _color2
_progressBarColor2 = _colorG - 15% // rgba(255, 255, 255, .1)
_progressBarColor3 = rgba(255, 255, 255, .2)
_progressBarSize = 1em

// @note La balise HTML progress posait des problèmes d'harmonisation des styles entre navigateurs, nous l'utilisons désormais depuis le 22/08/2023 en remplacement d'une solution constituée uniquement de divs. Les styles ont été adaptés pour chaque navigateur afin d'atteindre leur shadow DOM respectifs, en particulier pour l'animation @todo À évaluer.

.progress
  --color: _progressBarColor1
  -webkit-appearance: none
  -moz-appearance: none
  appearance: none
  width: 100%
  height: _progressBarSize
  border: none
  animation: anim-progress 4s linear infinite

.progress
.progress::-webkit-progress-bar
  background-color: _progressBarColor2
  box-shadow: inset 0 .2em .5em rgba(0, 0, 0, .2)
  border-radius: (_progressBarSize / 2)

.progress::-webkit-progress-inner-element
.progress::-webkit-progress-bar
  background-position: inherit

_progressBar =
  min-width: _progressBarSize
  background-color: var(--color)
  background-image: repeating-linear-gradient(-45deg, transparent 0 .5rem, _progressBarColor3 .5rem 1rem)
  background-size: calc(1rem / 0.707) 100%, 100% 800% // @note "0.707" = Cosinus de 45deg.
  background-position: inherit
  box-shadow: inset 0 2px 9px rgba(255, 255, 255, .3), inset 0 -2px 6px rgba(0, 0, 0, .4)
  border-radius: (_progressBarSize / 2)
  
.progress::-webkit-progress-value
  {_progressBar}

.progress::-moz-progress-bar
  {_progressBar}

@keyframes anim-progress
  0%
    background-position: 0 0
  100%
    background-position: calc(10 * 1rem / 0.707) 100% // @note "0.707" = Cosinus de 45deg.

/* Ancienne méthode, avec une div pour simuler la barre de progression, celle-ci étant pilotée par JavaScript :
.progress-bar
  --color: _progressBarColor1
  position: relative
  height: _progressBarSize
  background-color: _progressBarColor2
  box-shadow: inset 0 .2em .5em rgba(0, 0, 0, .2)
  border-radius: round(_progressBarSize / 2, 1)
  & div
  & div::after
    position: absolute
    border-radius: round(_progressBarSize / 2, 1)
  & div
    height: 100%
    min-width: _progressBarSize
    max-width: 100%
    background-color: var(--color)
    box-shadow: inset 0 2px 9px rgba(255, 255, 255, .3), inset 0 -2px 6px rgba(0, 0, 0, .4)
  & div::after
    content: ''
    z-index: 1
    inset: 0 //top 0 ; left 0 ; bottom 0 ; right 0
    background-image: linear-gradient(-45deg, _progressBarColor3 25%, transparent 25%, transparent 50%, _progressBarColor3 50%, _progressBarColor3 75%, transparent 75%, transparent)
    background-size: 2rem 2rem // @note Effet scalable non souhaité.
    animation: anim-progress-2 1.5s linear infinite

@keyframes anim-progress-2
  0%
    background-position: 0 0
  100%
    background-position: 2rem 2rem
*/
